version: "2"
run:
  tests: true
linters:
  default: none
  enable:
    # Tier 1: Bug prevention
    - bodyclose
    - contextcheck
    - errcheck
    - errorlint
    - exhaustive
    - forcetypeassert
    - govet
    - makezero
    - nilerr
    - nilnil
    - noctx
    - reassign
    - rowserrcheck
    - sqlclosecheck
    - wastedassign
    # Tier 2: Valuable quality
    - gocritic
    - ineffassign
    - misspell
    - modernize
    - nestif
    - nolintlint
    - unparam
    - unused
    # Tier 3: Kept but tuned
    - gosec
    - revive
    - staticcheck
  settings:
    errorlint:
      errorf: true
      asserts: true
      comparison: true
    gocritic:
      enabled-checks:
        - nilValReturn
        - deferInLoop
        - hugeParam
        - rangeValCopy
        - sloppyReassign
        - weakCond
    gosec:
      confidence: medium
    govet:
      enable-all: true
    misspell:
      locale: US
    nestif:
      min-complexity: 8
    nolintlint:
      require-explanation: true
      require-specific: true
    revive:
      rules:
        - name: unexported-return
        - name: error-strings
        - name: error-naming
        - name: if-return
        - name: increment-decrement
        - name: var-naming
    staticcheck:
      checks:
        - all
        - "-ST1000"
  exclusions:
    generated: lax
    rules:
      - linters:
          - gosec
          - noctx
        path: _test\.go
      # "cancelled" is GitHub's and GitLab's own literal check-conclusion API
      # vocabulary (double-L, not US spelling) — Check.Conclusion must
      # reproduce it verbatim to match what callers compare against, so it is
      # not a typo for misspell's US locale check to flag.
      - linters:
          - misspell
        path: internal/forge/gitlab(_test)?\.go
        text: '`cancelled` is a misspelling of `canceled`'
      # modernize's embedlit suggests dropping the embedded field's type name
      # from WorkerPlan{Worker: Worker{...}}-shaped literals, but Go only
      # allows that elision inside array/slice/map literals, never for a
      # struct field's value — applying it produces "unknown field" compile
      # errors whenever the struct (like WorkerPlan) has sibling fields
      # beside the embedded one. A genuine false positive for this shape.
      - linters:
          - modernize
        text: 'embedlit:'
    paths:
      - third_party$
      - builtin$
      - examples$
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
formatters:
  enable:
    - gci
    - gofmt
  settings:
    gci:
      sections:
        - standard
        - default
        - prefix(github.com/Elysium-Labs-EU/argus)
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
